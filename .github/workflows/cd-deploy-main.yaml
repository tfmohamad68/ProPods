# DEPRECATED: This workflow has been replaced by deploy-production.yaml
# The new workflow builds and pushes Docker images directly without external dependencies
# Delete this file after confirming the new workflow works
